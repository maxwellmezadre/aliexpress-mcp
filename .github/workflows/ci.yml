name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    name: Type-check, test & invariants (Bun)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      # verify.ts = tsc --noEmit + bun test + the real MCP server over stdio.
      # No network: there is no session, so no request is even attempted, and
      # the gated live test skips itself.
      - run: bun run scripts/verify.ts

  binary:
    name: Build binary
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile
      - run: bun run build:dist
      - run: bun run build:binary
      - run: ./aliexpress --version
